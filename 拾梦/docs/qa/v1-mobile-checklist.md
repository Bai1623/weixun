# 拾梦 V1 移动端验收清单

验收日期：2026-08-25

自动化覆盖 Chromium 桌面与 Pixel 5，另以 390 × 844 视口目视检查首次引导和设置页。标记“待真机”的项目需要真实 Android 或 iPhone 的系统能力，不能由桌面模拟器代替。

## 通用回归

| 检查项 | Android Chrome | iPhone Safari / 主屏幕 | 操作与通过标准 |
| --- | --- | --- | --- |
| 首次说明 | 自动化通过 | 待真机 | 首次访问先展示本机存储、备份与 AI 默认关闭说明；确认后进入梦河。 |
| 安全区域 | 模拟通过 | 待真机 | 顶部标题、底部导航和弹窗不被刘海、圆角或 Home Indicator 遮挡。 |
| 360px 横向溢出 | 自动化通过 | 自动化通过 | 页面根节点 `scrollWidth` 与 360px 视口一致，不出现横向滚动。 |
| 软键盘 | 待真机 | 待真机 | 输入标题、正文和关键词时，当前字段与“保存梦境”仍可触达；收起键盘后布局恢复。 |
| 焦点与键盘 | 自动化通过 | 待真机 | AI、恢复、长图弹窗打开后焦点进入弹窗，Tab 不逃逸，关闭后回到触发按钮。 |
| 减少动态 | 自动化设置通过 | 待真机 | “跟随系统 / 减少动态 / 保留动态”即时生效；减少动态时页面切换无位移动画。 |

## 本地数据与媒体

| 检查项 | Android Chrome | iPhone Safari / 主屏幕 | 操作与通过标准 |
| --- | --- | --- | --- |
| 文字草稿 | 自动化通过 | 自动化通过 | 输入后等待 400ms，刷新仍存在；保存后在梦河和档案可见。 |
| 麦克风授权 | 待真机 | 待真机 | 首次录音出现系统授权；拒绝后给出可理解提示，文字记录仍可使用。 |
| 暂停与继续录音 | 待真机 | 待真机 | 录音可暂停、继续、完成或取消，完成后出现本地播放器。 |
| 重启后播放 | 待真机 | 待真机 | 关闭并重新打开浏览器或主屏幕 App，已保存录音仍能播放。 |
| 空间警告 | 待真机 | 待真机 | 存储不足时保留页面内容并提示；设置页显示用量、配额及媒体数量。 |
| 完整备份下载 | 自动化通过 | 待真机 | 导出前提示“备份未加密”；下载 ZIP 包含文字和媒体。 |
| 跨新环境恢复 | 自动化通过 | 待真机 | 先显示恢复预览，再合并写入；原有数据不被清空，相同 ID 冲突不静默覆盖。 |

## 安装、离线与更新

| 检查项 | Android Chrome | iPhone Safari / 主屏幕 | 操作与通过标准 |
| --- | --- | --- | --- |
| 安装提示 | 待真机 | 不适用 | 只有点击“安装到桌面”后才出现系统安装确认。 |
| iOS 安装说明 | 不适用 | 待真机 | 未安装时显示“分享 → 添加到主屏幕”，从主屏幕打开为独立窗口。 |
| 离线重载 | 自动化通过 | 待真机 | 首次在线加载后断网并刷新，梦河、记录页和本机保存仍可使用。 |
| 离线边界 | 自动化通过 | 待真机 | Service Worker 只缓存应用壳；AI 请求、IndexedDB 内容、媒体和导出文件不进入运行时缓存。 |
| 编辑中更新 | 自动化逻辑通过 | 待真机 | 草稿未落盘或录音处于活动状态时，更新按钮不刷新页面；保存或结束录音后才能更新。 |

## AI 可选能力

| 检查项 | Android Chrome | iPhone Safari / 主屏幕 | 操作与通过标准 |
| --- | --- | --- | --- |
| 默认离线可用 | 自动化通过 | 自动化通过 | 未配置端点时不发出 AI 请求，记录、氛围封面、备份和长图正常工作。 |
| 服务地址校验 | 自动化通过 | 自动化通过 | 远程地址必须为 HTTPS；只为本地开发放行 localhost 与 127.0.0.1。 |
| 逐次确认 | 自动化通过 | 自动化通过 | 取消整理、转写或生图时请求数为 0；确认框明确列出本次发送的数据。 |
| 非破坏应用 | 单元测试通过 | 单元测试通过 | 整理先预览且不覆盖原始正文；转写确认后追加；替换图片失败时保留旧图片。 |

## 真机记录

真机验收时记录设备型号、系统版本、浏览器版本、通过/失败项及截图。失败项需要附最短复现步骤，并确认是否只在浏览器标签页或主屏幕安装模式出现。
